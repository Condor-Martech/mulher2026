---
import Container from "./ui/Container.astro";
import { Image } from "astro:assets";
import bannerImg from "../assets/banner.png";
import MapPin from "./icons/MapPin.astro";
import introData from "../data/intro.json";

const { text, branding, cta, location } = introData;
---

<section
  id="intro"
  class="pt-4 pb-8 md:py-12 bg-transparent relative overflow-hidden"
>
  <Container>
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-16 items-center">
      {/* Image Section */}
      <div class="relative group">
        <div
          class="absolute -inset-4 bg-primary/10 rounded-[2.5rem] opacity-30 group-hover:opacity-50 blur-2xl transition-opacity"
        >
        </div>
        <div
          class="relative rounded-3xl overflow-hidden shadow-premium border-2 border-primary/10 lg:-rotate-1"
        >
          <Image
            src={bannerImg}
            alt="Mês da Mulher Condor"
            width={800}
            height={600}
            class="w-full h-auto transition-transform duration-700 group-hover:scale-105"
          />
        </div>
      </div>

      {/* Content Section */}
      <div class="flex flex-col space-y-6 md:space-y-8">
        <div class="relative">
          <p
            class="text-xl md:text-2xl lg:text-3xl text-accent/80 leading-relaxed font-outfit font-medium tracking-tight"
            set:html={text}
          />
        </div>

        {/* Call to Action Logo Branding */}
        <div class="relative pt-4 md:pt-6">
          <div
            class="absolute inset-0 bg-primary/10 rounded-[3rem] blur-3xl -z-10"
          >
          </div>
          <div class="relative">
            <div
              class="relative flex flex-col items-center lg:items-start lg:translate-x-8"
            >
              <h2
                class="text-5xl md:text-6xl lg:text-[6rem] font-script text-white drop-shadow-sm leading-none"
              >
                {branding.first}
              </h2>
              <div
                class="flex items-center gap-4 lg:gap-6 -mt-2 ml-12 md:ml-24 lg:ml-24"
              >
                <span
                  class="font-script text-xl md:text-2xl lg:text-5xl text-accent/20 italic"
                  >{branding.separator}
                </span>
                <h2
                  class="text-5xl md:text-6xl lg:text-[6rem] font-script text-white drop-shadow-sm leading-none"
                >
                  {branding.second}
                </h2>
              </div>
            </div>
          </div>
        </div>

        <div class="pt-6 md:pt-8">
          <h3
            class="text-2xl md:text-3xl font-bold font-outfit text-[#5A4A4A] mb-6 leading-tight"
          >
            {cta}
          </h3>

          <div
            class="flex items-center gap-4 bg-[#FFF1F2] px-6 py-4 rounded-3xl border border-primary/10 shadow-sm hover:translate-y-[-2px] transition-all duration-300 group max-w-xl"
          >
            <div
              class="w-12 h-12 bg-white rounded-2xl flex items-center justify-center text-[#E95D79] shadow-sm shrink-0 group-hover:scale-110 transition-transform"
            >
              <MapPin class="w-7 h-7" fill={true} />
            </div>
            <p
              class="text-sm md:text-base font-medium text-[#E95D79] leading-tight"
            >
              <span class="font-black">{location.split("|")[0]}</span>
              <span class="opacity-80">| {location.split("|")[1]}</span>
            </p>
          </div>
        </div>
      </div>
    </div>
  </Container>
</section>
